# Tools

This contains misc. tools, unrelated to the main project.

- `resize_images.py` will find and resize images to be a max of 400 pixels wide while maintaining the image ratio. Used for assets in the documentation of this project. 